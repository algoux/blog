---
type Metric = {
  label: string;
  value: string;
};

type Props = {
  metrics: Metric[];
};

const { metrics } = Astro.props;
---

<div class="metric-strip">
  {metrics.map((metric) => (
    <div>
      <strong>{metric.value}</strong>
      <span>{metric.label}</span>
    </div>
  ))}
</div>

